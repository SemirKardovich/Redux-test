import Navigation from './Navigation'

const Navbar = () => {
    return (
        <nav className='nav-bar'>
            <h1 style={{flex: '2', color: 'white'}}>JSON PLACEHOLDER</h1>
            <Navigation />
        </nav>
    )
}

export default Navbar
